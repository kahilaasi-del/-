# CLAUDE.md — הנחיות עבודה לפרויקט

הנחיות ל‑Claude בעבודה על מאגר זה (חשבשבת BI — ניתוח מכירות, יעדים ודשבורד).

## העדפות קבועות (חשוב)

- **חתך סוכן תמיד:** בכל תוצר — קובץ יעדים, דוח מכירות, סיכום, טבלה או גרף —
  יש לכלול **חתך לפי סוכן** כברירת מחדל, גם אם לא התבקש במפורש. שורה/סדרה לכל סוכן.
- **עברית + RTL:** התוצרים בעברית. גיליונות Excel עם `sheet_view.rightToLeft = True`.
- **נוסחאות חיות:** בקובצי Excel יש להשאיר תאי הנחה (אחוז גידול, ימים שחלפו וכו')
  פתוחים לעריכה, כך שכל השאר מתעדכן אוטומטית. להפעיל `wb.calculation.fullCalcOnLoad = True`.

## מתודולוגיית יעדים

- **בסיס יעד:** מכירות בפועל בתקופה המקבילה בשנה הקודמת.
- **יעד:** בסיס × (1 + אחוז גידול). ברירת מחדל **25%** (תא הנחה ניתן לשינוי).
- **תחזית עמידה ביעד (run‑rate):** לחודש חלקי משליכים לחודש מלא לפי קצב:
  `תחזית = חודש שהושלם + (חודש חלקי × ימים בחודש / ימים שחלפו)`.
- **מקרא הערכה:** ירוק ≥ 100% מהיעד · צהוב 85%–100% ("על הגבול") · אדום < 85% ("בסיכון").

## נתונים

- מקור עיקרי: ייצוא מכירות חודשי מחשבשבת (CSV), עמודות כולל `שם סוכן`, `חודשים`
  (בפורמט `MM/YYYY`), `כמות`, `מכירות`, `רווח`.
- סכומים כספיים ב‑₪, פורמט `#,##0`.

## אבטחה

- אין להכניס פרטי חיבור/סיסמאות ל‑git. סודות נשמרים ב‑`.env` בלבד (ראה `.gitignore`).
- כל הגישה למסד הנתונים היא **קריאה בלבד** (`SELECT`).
